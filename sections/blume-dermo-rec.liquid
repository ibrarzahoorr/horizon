{%- style -%}
  .dermo-wrapper {
    padding: 80px 0;
    background: #f8f9fa;
  }
  .dermo-split {
    display: flex;
    align-items: center;
    gap: 60px;
  }
  .dermo-content {
    flex: 1;
    max-width: 500px;
  }
  .dermo-content h3 {
    font-family: var(--font-heading--family);
    font-size: 42px;
    font-weight: 600;
    margin-bottom: 25px;
    color: #1a3a5c;
    line-height: 1.2;
  }
  .dermo-quote {
    font-size: 18px;
    line-height: 1.7;
    color: #1a3a5c;
    margin-bottom: 30px;
    font-style: italic;
  }
  .dermo-doctor-name {
    font-size: 16px;
    font-weight: 700;
    color: #1a3a5c;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 5px;
  }
  .dermo-doctor-role {
    font-size: 15px;
    color: #555;
  }
  .dermo-image {
    flex: 1;
    max-width: 500px;
  }
  .dermo-image img {
    width: 100%;
    height: auto;
    display: block;
  }
  @media (max-width: 991px) {
    .dermo-split {
      flex-direction: column;
      gap: 40px;
    }
    .dermo-content, .dermo-image {
      max-width: 100%;
    }
    .dermo-content h3 {
      font-size: 32px;
    }
  }
{%- endstyle -%}

<section class="dermo-wrapper">
  <div class="modern-container">
    <div class="dermo-split">
      <div class="dermo-content">
        <h3>{{ section.settings.title }}</h3>
        <div class="dermo-quote">
          {{ section.settings.text }}
        </div>
        <div class="dermo-doctor-name">{{ section.settings.doctor_name }}</div>
        <div class="dermo-doctor-role">{{ section.settings.doctor_role }}</div>
      </div>
      <div class="dermo-image">
        {%- if section.settings.image != blank -%}
          {{ section.settings.image | image_url: width: 800 | image_tag: loading: 'lazy' }}
        {%- else -%}
          {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Dermatologist Recommended",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Dermatologist Recommended"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Paragraph/Quote",
      "default": "Blume products focus on hydration to protect the skin barrier, yet also deliver gentle active ingredients to benefit the skin. Blume's emphasis on moisturizing ingredients and gentle actives means you get the most out of each product."
    },
    {
      "type": "text",
      "id": "doctor_name",
      "label": "Doctor Name",
      "default": "DR. LEELA ATHALYE"
    },
    {
      "type": "text",
      "id": "doctor_role",
      "label": "Doctor Role/Title",
      "default": "Board Certified Dermatologist"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Doctor Image"
    }
  ],
  "presets": [
    {
      "name": "Dermatologist Recommended"
    }
  ]
}
{% endschema %}
