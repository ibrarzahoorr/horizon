<div class="best-seller-card" {{ block_attributes }}>
  <a href="{{ product.url }}" class="best-seller-image-wrapper">
    {%- if product.featured_image != blank -%}
      {{ product.featured_image | image_url: width: 600 | image_tag: loading: 'lazy', alt: product.title }}
    {%- else -%}
      {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
    {%- endif -%}
    
    {%- if badge_1 != blank or badge_2 != blank -%}
      <div class="best-seller-badges">
        {%- if badge_1 != blank -%}
          {{ badge_1 | image_url: width: 100 | image_tag: class: 'best-seller-badge', alt: 'Badge' }}
        {%- endif -%}
        {%- if badge_2 != blank -%}
          {{ badge_2 | image_url: width: 100 | image_tag: class: 'best-seller-badge', alt: 'Badge' }}
        {%- endif -%}
      </div>
    {%- endif -%}
    
    <div class="best-seller-hover-btn">
      Add to cart - {{ product.price | money }}
    </div>
  </a>
  
  <div class="best-seller-info">
    <h3 class="best-seller-title">{{ product.title }}</h3>
    
    {%- if product.metafields.reviews.rating.value != blank -%}
      <div class="best-seller-rating">
        <span class="best-seller-stars">
          {%- for i in (1..5) -%}
            {%- if i <= product.metafields.reviews.rating.value -%}
              ★
            {%- else -%}
              ☆
            {%- endif -%}
          {%- endfor -%}
        </span>
        <span class="best-seller-review-count">({{ product.metafields.reviews.rating_count | default: 0 }})</span>
      </div>
    {%- endif -%}
    
    <p class="best-seller-description">{{ custom_description | default: product.description | strip_html | truncatewords: 12 }}</p>
    
    <div class="best-seller-price">
      {%- if product.compare_at_price > product.price -%}
        <span class="sale-price">{{ product.price | money }}</span>
        <span class="compare-price">{{ product.compare_at_price | money }}</span>
      {%- else -%}
        {{ product.price | money }}
      {%- endif -%}
    </div>
  </div>
</div>
