{{ u-cx | strip_newlines | lstrip | rstrip | replace: '  ', ' ' | strip }}
{% comment %}
  Use this to format strings for classnames, etc, e.g.

  {% capture cx %}
    button rel sans caps track
    {{ className }}
    {% if outline == true %}button--outline{% endif %}
    {% if small == true %}button--small{% endif %}
    {% if light == true %}button--light{% endif %}
  {% endcapture %}

  {% include 'u-cx' with cx %}
{% endcomment %}

{% assign cx = 'null' %}
